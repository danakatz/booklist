<p>On <%= @comment.date %>, <%= @comment.user.name %> wrote:</br>
<%= @comment.text %></p>
<%= link_to 'Edit comment', edit_comment_url(@comment.id) %></br>
<%= link_to 'Delete comment', delete_comment_url(@comment.id), :method => :delete %>